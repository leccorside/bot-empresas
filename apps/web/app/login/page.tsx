'use client';
import {FormEvent,useState} from 'react';import {api} from '../../lib/api';
export default function Login(){const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);try{const result=await api('auth/login',{method:'POST',body:JSON.stringify({email:data.get('email'),password:data.get('password')})});localStorage.setItem('token',result.token);location.href='/'}catch(e:any){setError(e.message)}}return <div className="login"><div className="card loginCard"><div className="logo"><span className="logoMark">LP</span>Local Prospector</div><h1>Bem-vindo de volta</h1><div className="sub">Acesse seu servidor local de prospecção.</div><form onSubmit={submit}><div className="field"><label>E-MAIL</label><input className="input" name="email" defaultValue="admin@local.test" /></div><div className="field"><label>SENHA</label><input className="input" name="password" type="password" defaultValue="prospector" /></div>{error&&<div style={{color:'var(--danger)',fontSize:13}}>{error}</div>}<button className="btn">Entrar no painel</button></form></div></div>}
